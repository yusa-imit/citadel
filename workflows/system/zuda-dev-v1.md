CRITICAL: First read/increment `.claude/session-counter` to determine mode. counter % 5 == 0 → STABILIZATION (CI, issues, tests, cross-compile, code quality). Other runs → FEATURE (implement Phase 1 from docs/PRD.md: scaffolding → lists/queues → hash containers → heaps). This is a LIBRARY — allocator-first, comptime-parameterized, no @panic, no stdout. Every container needs: comptime generic params, init/deinit, insert/remove/get, iterator with next()->?T, validate(), Big-O doc comments, thorough tests. Check consumer use cases in CLAUDE.md before implementing. Every cycle must produce at least one commit with real code changes. NEVER use EnterPlanMode/ExitPlanMode.
