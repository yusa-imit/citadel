CRITICAL: First read/increment `.claude/session-counter` → counter % 5 == 0 = STABILIZATION (CI, tests, security audits), else FEATURE (implement next item from .claude/memory/project-context.md following phases 1→6). If no root Cargo workspace exists, bootstrap Phase 1 first (hexagonal skeleton + React/Vite + WASM stub). SECURITY IS NON-NEGOTIABLE: server never sees plaintext; only openmls/opaque-ke/RustCrypto; crypto code must pass crypto-reviewer, architecture must pass threat-model-checker, backend must pass security-auditor — run them in-session via Task BEFORE committing. Every cycle must produce at least one commit+push with real changes. NEVER use EnterPlanMode/ExitPlanMode. Use `git add <specific files>`, never `git add -A`. End with a Discord summary to user:264745080709971968.
