CRITICAL: First read/increment `.claude/session-counter` to determine mode. counter % 5 == 0 → STABILIZATION (CI, issues, integration tests, bugs). Other runs → FEATURE (implement Phase 9-13 from docs/PRD.md following dependency graph). ALWAYS check CI first in both modes — if CI is red, fix it regardless of mode. Every cycle must produce at least one commit with real changes. NEVER use EnterPlanMode/ExitPlanMode.
