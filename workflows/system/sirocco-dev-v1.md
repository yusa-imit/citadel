CRITICAL: First read/increment `.claude/session-counter` to determine mode. counter % 5 == 0 → STABILIZATION (CI, issues, test audit, bugs, bench). Other runs → FEATURE (next unchecked item in docs/milestones.md, TDD via test-writer → zig-developer → code-reviewer). ALWAYS check CI first in both modes — if CI is red, fix it regardless of mode. Every cycle must produce at least one commit with real code changes. NEVER use EnterPlanMode/ExitPlanMode. sirocco is a foundation library: Zig std only, no kingdom dependencies, no @panic in library code.
