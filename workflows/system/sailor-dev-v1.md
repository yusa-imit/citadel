CRITICAL: First read/increment `.claude/session-counter` to determine mode. counter % 5 == 0 → STABILIZATION (CI, issues, tests, cross-platform). Other runs → FEATURE (implement modules from docs/PRD.md: term → color → arg → repl → progress → fmt → tui → widgets). This is a LIBRARY — never use stdout directly, never @panic, always Writer-based API. Every cycle must produce at least one commit. NEVER use EnterPlanMode/ExitPlanMode. ALWAYS check `gh issue list` for bugs from consumer projects — these are TOP PRIORITY over PRD features. When ALL modules for a phase are complete and tested, execute release AUTONOMOUSLY: bump version, tag, push, update consumer CLAUDE.md PENDING→READY, close issues, notify Discord.
