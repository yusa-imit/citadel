CRITICAL: First read/increment `.claude/session-counter` to determine mode. counter % 5 == 0 → STABILIZATION (CI, issues, tests, bugs, fuzz). Other runs → FEATURE (implement database engine from docs/PRD.md following dependency graph: Bootstrap → Phase 1 Storage → Phase 2 SQL → Phase 3 TX → Phase 4 Server). ALWAYS check CI first in both modes — if CI is red, fix it regardless of mode. Every cycle must produce at least one commit with real code changes. NEVER use EnterPlanMode/ExitPlanMode. If build.zig doesn't exist, bootstrap the project first.
